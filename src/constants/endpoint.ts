const API_VERSION = "/api/v1";

// AUTH
export const LOGIN = `${API_VERSION}/auth/login`;
export const REGISTER = `${API_VERSION}/auth/register`;
export const SEND_VERIFY_CODE = `${API_VERSION}/verification/send-code`;
export const VERIFY_CODE = `${API_VERSION}/verification/verify-code`;
